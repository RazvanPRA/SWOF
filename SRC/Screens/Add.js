import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ADD = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default ADD;

const styles = StyleSheet.create({});
